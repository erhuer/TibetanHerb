# 本项目下的dataset中的数据集仅供大家参考存放格式和调试代码是否能跑通
# dataset path
#path: ultralytics-yolo11-main/
train: ship/images/train  # path + train
val: ship/images/val # path + val
test: ship/images/test # path + test

# number of classes
nc: 1

# class names
names: ['ship']